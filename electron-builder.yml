appId: 'chillmon.launcher'
productName: 'Chillmon'
artifactName: '${productName}-setup-${version}.${ext}'

copyright: 'Copyright В© Chillmon'

asar: true
compression: 'normal'

files:
  - '!{dist,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,eslint.config.mjs}'

extraResources:
  - 'libraries'
  - from: '../run-cobblemon-pack/distribution.json'
    to: 'distribution.json'

# Windows Configuration
win: 
  icon: build/icon.ico
  # false: winCodeSign extract needs symlink privilege on this machine.
  # Icon is applied in build/afterPack.js via @electron/rcedit instead.
  signAndEditExecutable: false
  target: 
    - target: 'nsis'
      arch: 'x64'
    - target: 'portable'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  installerHeaderIcon: build/icon.ico
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Chillmon

# macOS Configuration
mac:
  target:
    - target: 'dmg'
      arch:
        - 'x64'
        - 'arm64'
  artifactName: '${productName}-setup-${version}-${arch}.${ext}'
  category: 'public.app-category.games'

# Linux Configuration
linux: 
  target: 'AppImage'
  maintainer: 'Daniel Scalzi'
  vendor: 'Daniel Scalzi'
  synopsis: 'Modded Minecraft Launcher'
  description: 'Custom launcher which allows users to join modded servers. All mods, configurations, and updates are handled automatically.'
  category: 'Game'


afterPack: build/afterPack.js

directories:
  buildResources: 'build'
  output: 'dist'
